# Docker Usage

## Build and Run
1. Create a .env file for runtime configuration.
2. Build and start the container:
   docker compose up --build
3. Open the app:
   http://localhost:3000

## Notes
- This container runs the Next.js app in production mode.
- For local development, you can still use the normal Next.js dev server.
