// Stub for Next.js's "server-only" module — vitest tests run in the browser
// (jsdom) and don't need the real server-only guard. If a test ever
// accidentally imports a server-only module the implementation will still
// run, but bundlers will be configured to throw at build time.
export {};
